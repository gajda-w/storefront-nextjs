fragment CartLine on CheckoutLine {
  id
  quantity
  variant {
    id
    name
    product {
      id
      name
      thumbnail {
        url
      }
    }
    pricing {
      price {
        net {
          amount
        }
      }
    }
  }
}
